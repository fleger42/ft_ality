type action = (string * string)

type automaton = {
  alphabet: string list;
  action_list: action list;
  states: string list;
  combos: (string * string list);
}

parse_data:
return automaton

execution (automaton_data):


main:
automaton_data = parse_data
execution automaton_data